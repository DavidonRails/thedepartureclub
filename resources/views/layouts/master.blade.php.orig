<!doctype html>
<html lang="en">
<head>
    <base href="{{ url() }}">
    <title>Hobojet</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">

    @if(Config::get('app.debug'))
        <link rel="stylesheet" href="{{ url('css/app.css') }}">
    @else
        <link rel="stylesheet" href="{{ url('build/css/app.css') }}">
    @endif

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <script>
        var fbid = '{{getenv('FACEBOOK_APP_ID')}}'
    </script>
</head>
<body ng-app="app" ng-controller="AppController">

<button class="menu-btn visible-md visible-sm visible-xs">Menu</button>

<header>
    <div class="logo">
        <a href="#">
            <img src="images/logo.svg" alt="">
        </a>
    </div>
    <div class="main-menu">
        <ul>
            <li>
                <a class="{{{(Request::is('jet-deals') ? 'active' : '')}}}" href="{{url('jet-deals')}}">Jet Deals</a>
            </li>
            <li>
                <a class="{{{(Request::is('my-trips') ? 'active' : '')}}}" href="{{url('my-trips')}}">My Trips</a>
            </li>
            <li>
                <a class="{{{(Request::is('on-demand') ? 'active' : '')}}}" href="{{url('on-demand')}}">Jet On Demand</a>
            </li>
            <li>
                <a class="{{{(Request::is('trip-alerts') ? 'active' : '')}}}" href="{{url('trip-alerts')}}">Trip Alerts</a>
            </li>
            <li>
                <a class="{{{(Request::is('packs') ? 'active' : '')}}}" href="{{url('packs')}}">Hobo Pack</a>
            </li>
            <li>
                <a class="{{{(Request::is('membership') ? 'active' : '')}}}" href="{{url('membership')}}">Membership</a>
            </li>
        </ul>
    </div>

    <div class="user-menu">
        <ul>
            @if (Auth::check())
                @if (Auth::getUser()->role == 'admin' || Auth::getUser()->role == 'operator')
                    <li>
                        <a href="{{ url('admin/') }}">
                            @if(Auth::getUser()->role == 'operator')
                                Dashboard
                            @endif
                            @if(Auth::getUser()->role == 'admin')
                                Admin
                            @endif
                        </a>
                    </li>
                @endif
                <li><a href="{{ url('profile') }}">Profile</a></li>
                <li><a href="{{ url('auth/logout') }}">Logout</a></li>
            @endif
            @if (!Auth::check())
                <li>
                    <a href="javascript:void(0)"
                       data-template-url="login-modal.html"
                       bs-modal
                       data-animation="am-fade"
                       data-container="body" >
                        Login
                    </a>
                </li>
                <li>
                    <a href="javascript:void(0)"
                       data-template-url="register-modal.html"
                       bs-modal
                       data-animation="am-fade"
                       data-container="body" >
                        Register
                    </a>
                </li>
            @endif
        </ul>
    </div>
    
    <button class="search-btn">
        <i class="ti-search"></i>
    </button>
</header>

@yield('body-content')

<<<<<<< HEAD
        <div class="hero-heading">
            <h1>Need a lift?</h1>
            <p>Search for empty one-way private flights and score an amazing deal!</p>
        </div>

        <div class="hero-search" ng-controller="flightSearch">
            <ul class="inline-hero-form">
                <li class="search-origin">
                    <input 
                        ng-model="originPlace" 
                        bs-options="origin for origin in origin" 
                        placeholder="Origin" 
                        data-animation="am-fade"
                        data-min-length="0"
                        data-limit="3"
                        bs-typeahead
                        type="text">
                        <i class="ti-location-arrow input-ico"></i>
                </li>
                <li class="search-destination">
                    <input                     
                        ng-model="destinationPlace" 
                        bs-options="destination for destination in destination"
                        placeholder="Destination"
                        data-animation="am-fade"
                        data-min-length="0"
                        data-limit="3"
                        bs-typeahead 
                        type="text">
                        <i class="ti-location-arrow input-ico"></i>
                </li>
                <li class="search-button">
                    <button class="btn btn-block btn-primary">Find a Jet</button>
                </li>
            </ul>
        </div>
    </div>

    <div class="app-link">
        Get our Free App
        <a href="#">
            <img src="images/icon-store.png" alt="">
        </a>
    </div>
    <div class="scroll-down">
        <a href="#">
            <img src="images/scroll-down.svg" alt="">
        </a>
        <ul class="vertical-dots">
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
        </div>
    </div>
</section>
=======
>>>>>>> frontend

<!-- <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">
                <img src="images/logo.svg" alt="">
                <i class="ti-close"></i>
            </a>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            @if (Auth::check())
            <ul class="nav navbar-nav navbar-right">
                @if (Auth::getUser()->role == 'admin' || Auth::getUser()->role == 'operator')
                    <li><a href="{{ url('admin/') }}">Admin</a></li>
                @endif
                    <li><a href="{{ url('profile') }}">Profile</a></li>
                    <li><a href="{{ url('auth/logout') }}">Logout</a></li>
            </ul>
            @endif
            @if (!Auth::check())
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ url('auth/login') }}">Login</a></li>
                <li><a href="{{ url('auth/register') }}">Register</a></li>
            </ul>
            @endif
        </div>
    </div>
</nav> -->

<div class="page-container">

    @foreach (Alert::getMessages() as $type => $messages)
        @foreach ($messages as $message)
            <div class="alert alert-{{ $type }}">{{ $message }}</div>
        @endforeach
    @endforeach

    @yield('content')
</div>

@if(Config::get('app.debug'))
    <script src="{{ url('js/dependencies.js') }}"></script>
@else
    <script src="{{ url('build/js/dependencies.js') }}"></script>
@endif

@if(Config::get('app.debug'))
    <script src="{{ url('js/app/app.js') }}"></script>
@else
    <script src="{{ url('build/js/app.js') }}"></script>
@endif
<<<<<<< HEAD
=======

<!-- footer -->
<footer>
<hr>
    ovo footer Lorem ipsum dolor sit amet, consectetur adipisicing elit. Consectetur a ut, voluptas atque quibusdam accusantium eos quo reprehenderit ab optio accusamus assumenda omnis minus animi enim quos eligendi mollitia nostrum?
</footer>

>>>>>>> frontend
@include("auth.ng-templates")

</body>
</html>